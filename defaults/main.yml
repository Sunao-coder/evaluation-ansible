#SPDX-License-Identifier: MIT-0
---
wordpress_db_name: wordpress
wordpress_db_user: wpuser
wordpress_db_password: wppassword
wordpress_db_host: localhost
mariadb_root_password: rootpassword

# WordPress configuration
wordpress_download_url: https://wordpress.org/latest.zip
wordpress_install_dir: /var/www/html
wordpress_admin_email: admin@localhost

# Apache configuration
wordpress_apache_document_root: /var/www/html
wordpress_apache_server_admin: admin@localhost
